notifications:
  email: false
 
language: c++
env:
  - CXX=g++-4.9
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.9
      
before_install:
  - git clone https://github.com/nlohmann/json.git

script:
  - make
  - ./cpal --help
 
